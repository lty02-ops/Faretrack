const test=require('node:test');const assert=require('node:assert/strict');const{DynamoDbWatchRepository}=require('../src/repositories/dynamoDb/dynamoDbWatchRepository');const{DynamoDbPriceHistoryRepository}=require('../src/repositories/dynamoDb/dynamoDbPriceHistoryRepository');
test('WatchTarget은 검색 키 기반 PK와 조건부 쓰기로 저장한다',async()=>{let input;const client={send:async command=>{input=command.input;return{};}};const repository=new DynamoDbWatchRepository(client,'Faretrack');const target={id:'watch_1',key:'ICN#NRT#20261010#20261013#ROUND#1',active:true,query:{origin:'ICN'}};await repository.saveTarget(target);assert.deepEqual(input.Key,undefined);assert.equal(input.Item.PK,`WATCH#${target.key}`);assert.equal(input.Item.SK,'META');assert.match(input.ConditionExpression,/attribute_not_exists/);});
test('PriceHistory는 시간 정렬 가능한 SK로 저장한다',async()=>{let input;const client={send:async command=>{input=command.input;return{};}};const repository=new DynamoDbPriceHistoryRepository(client,'Faretrack');await repository.save({id:'price_1',watchKey:'ROUTE',price:150000,checkedAt:'2026-08-18T00:00:00.000Z'});assert.equal(input.Item.PK,'WATCH#ROUTE');assert.equal(input.Item.SK,'PRICE#2026-08-18T00:00:00.000Z#price_1');});
