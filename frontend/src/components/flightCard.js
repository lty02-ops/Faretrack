import{formatWon}from'../utils/currency.js';
export function flightCard(flight,index,query){return`<article class="flight-card"><div class="airline"><div class="airline-logo">${flight.airline[0]}</div><div><strong>${flight.airline}</strong><span>${flight.flightNumber}</span></div></div><div class="route"><div><strong class="time">${flight.departureTime}</strong><span>${query.origin}</span></div><div class="route-line"><div class="stops">${flight.stops?'경유 '+flight.stops+'회':'직항'}</div></div><div><strong class="time">${flight.arrivalTime}</strong><span>${query.destination}</span></div></div><div class="seller"><span>판매처</span><strong>${flight.seller}</strong><button class="track" data-track="${index}">가격 추적하기</button></div><div class="price"><span>현재 확인 최저</span><strong>${formatWon(flight.price)}</strong><a class="buy" href="${flight.bookingUrl}" target="_blank" rel="noopener">판매처 이동 ↗</a></div></article>`;}
